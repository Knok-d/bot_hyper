# No [job-queue] extra: periodic work is a plain asyncio task (Bot._flush_loop),
# so APScheduler isn't needed. PTB logs one warning about it at startup.
python-telegram-bot>=21.0
websockets>=12.0
httpx>=0.27
certifi>=2024.2.2
aiosqlite>=0.20.0
python-dotenv>=1.0.0

# dev / test
pytest>=8.0
pytest-asyncio>=0.23
